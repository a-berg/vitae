%% This is a template for pandoc usage
\cvitem
    {${it.jobtitle}}
    {${it.start} -- ${it.end} (${it.duration})}
    {${it.company}}
    {${it.location}}
\begin{itemize}
  \item ${ it.highlights[\item ] }
\end{itemize}
\textit{\textbf{Tools used}: ${ it.tools }.}
\vspace{12pt}\\
